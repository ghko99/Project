# Project

Machine learning classification template using pandas and scikit-learn.

## File

- `template.py`: example workflow for loading data, splitting train/test sets, fitting classifiers, and reporting metrics.

## Setup

```bash
pip install -r requirements.txt
```

## Run

Update the input data path in `template.py`, then run:

```bash
python template.py
```
